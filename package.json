{
  "name": "glabscope",
  "module": "src/index.tsx",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun src/index.tsx",
    "test": "bun test",
    "build": "bun build --compile src/index.tsx --no-compile-autoload-dotenv --outfile dist/glabscope",
    "install:local": "bash scripts/install.sh",
    "uninstall:local": "bash scripts/uninstall.sh"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.3.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@opentui/core": "^0.5.11",
    "@opentui/react": "^0.5.11",
    "react": "^19.3.0"
  }
}

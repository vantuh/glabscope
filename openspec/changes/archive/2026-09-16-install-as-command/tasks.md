## 1. Build command

- [x] 1.1 Add `"build": "bun build --compile src/index.tsx --no-compile-autoload-dotenv --outfile dist/glabscope"` to the `scripts` block in `package.json`; verify with `bun run build` exiting 0, `ls -lh dist/glabscope` showing roughly 71 MB, and `git status --porcelain` still not listing `dist/` (it is already in `.gitignore`).
- [x] 1.2 Verify the built artifact carries no external native library: `otool -L dist/glabscope` lists only system libraries (`libSystem`, `libc++`, `libicu`, `libresolv`) with no path into `node_modules` or `/opt/homebrew`; record the actual output.
Recorded output: `/usr/lib/libicucore.A.dylib`, `/usr/lib/libresolv.9.dylib`, `/usr/lib/libc++.1.dylib`, `/usr/lib/libSystem.B.dylib` — four system libraries and nothing else.
- [x] 1.3 Verify the artifact runs and that `.env` autoloading is really off: start `dist/glabscope` from a scratch directory whose `.env` sets `GLAB_BIN=/bin/false` (use `script -q <log> ./dist/glabscope` so it has a PTY, then kill it after a few seconds); the captured output must show the framed `Not a git repository` error and must NOT show the missing-`glab` message — with dotenv autoloading on, the second is what would appear.
Verified 2026-09-16 with a control run in the same scratch directory: (A) with `.env` present and `GLAB_BIN` not exported, the framed `Not a git repository: fatal: not a git repository (or any of the parent directories): .git` appears — 1 occurrence — and the missing-`glab` message appears 0 times; (B) with `GLAB_BIN=/bin/false` exported instead, the missing-`glab` message appears and the git error does not, so the variable does reach the process and (A) is a real result rather than a dead check.

## 2. Installer

- [x] 2.1 Write `scripts/install.sh`: resolve the project root from the script's own location, default `PREFIX=${PREFIX:-$HOME/.local/bin}`, fail with a non-zero exit before touching anything when the build tool is missing, warn without failing when `glab` is missing, run `bun run build`, `mkdir -p "$PREFIX"`, `ln -sf "$ROOT/dist/glabscope" "$PREFIX/glabscope"`, then print the `PATH` line when `$PREFIX` is not on `$PATH`; verify with `PREFIX=$(mktemp -d) bash scripts/install.sh` exiting 0 and `ls -l "$PREFIX/glabscope"` showing the link into this project's `dist/`.
Exit code 0 with the link pointing at this project's `dist/glabscope`. Two review-driven differences from the literal recipe, both verified: the link is made with `ln -sfn`, because plain `ln -sf` follows an entry that is a symlink to a directory and creates the link *there* — a measured write outside `PREFIX` — and a real directory at the entry path is refused before the build instead of receiving a nested link and reporting success. The project root is resolved with `pwd -P` so the installed target is the physical path.
- [x] 2.2 Verify idempotence and the custom target: run the same command a second time and confirm exit 0 with exactly one entry remaining (`ls -1 "$PREFIX" | wc -l` is 1) and the link still pointing at this project's build; then run `PREFIX=$(mktemp -d)/nested bash scripts/install.sh` and confirm the nested directory is created and the link lands there.
- [x] 2.3 Verify the missing-prerequisite refusal: run `env PATH=/usr/bin:/bin bash scripts/install.sh` with a temp `PREFIX` and confirm a non-zero exit, a message naming the missing build tool, and that the target directory was left without an entry. The default `PATH` cannot be used for this check, since it always contains the build tool.
Exit code 1, first line `glabscope: bun is required to build the command but was not found on PATH.`, target directory left with 0 entries.
- [x] 2.4 Verify the `glab` warning is not fatal: run the installer with the build tool's directory on `PATH` but `glab`'s directory removed (for example `env PATH="$HOME/.bun/bin:/usr/bin:/bin" bash scripts/install.sh`) and confirm the warning appears, the exit code is 0, and the entry is installed.
Exit code 0, `warning: glab is not on PATH` present once, entry installed.
- [x] 2.5 Verify a failed build leaves nothing pointed at a stale binary: temporarily set the `build` script in `package.json` to a command that exits non-zero, run the installer with a temp `PREFIX` against a target directory that already holds a link to a *deleted* artifact (`rm dist/glabscope` first), confirm the installer exits non-zero and reports the failure, then restore the `build` script and re-run to a clean state.
Exit code 1, `build failed; nothing was installed.` followed by `removed <PREFIX>/glabscope: this build did not produce <project>/dist/glabscope.`, and the target directory left with 0 entries. Both halves were run: with `dist/glabscope` deleted the entry was dangling, and with a *runnable previous artifact still on disk* the entry was removed all the same (`entries left in PREFIX: 0`), because the spec requires that a failed build leave no entry pointing at a stale binary, not merely a missing one. `drop_our_entry` therefore removes only a symlink whose target text is exactly this project's `dist/glabscope`; a foreign link, a regular file, or a directory is never touched (the same criterion the uninstaller uses). `package.json` was restored byte-identically and a re-run rebuilt the checkout.

## 3. Uninstaller

- [x] 3.1 Write `scripts/uninstall.sh`: `PREFIX=${PREFIX:-$HOME/.local/bin}`, resolve the project root the same way, read the entry with `readlink`, compare the target text against `"$ROOT/dist/glabscope"`, remove the entry only on a match, refuse with a non-zero exit and a reason for a regular file, a directory, or a link elsewhere, and report-and-exit-zero when nothing exists at that path; verify each of those five cases against a temp `PREFIX` and record the five exit codes.
The five exit codes, in order: own link removed `0`; regular file refused `1` (file untouched); directory refused `1` (directory untouched); foreign symlink refused `1` (link untouched, still pointing at `/bin/echo`); nothing installed `0`.
- [x] 3.2 Verify the dangling-link case: install into a temp `PREFIX`, delete `dist/glabscope`, run the uninstaller, and confirm it removes the entry and exits 0; then run `bun run build` again and reinstall so the checkout is left in a working state.
- [x] 3.3 Verify the uninstaller leaves the project alone: after an uninstall, confirm `git status --porcelain` lists no change under `dist/` or `scripts/` and that `PREFIX=$(mktemp -d) bash scripts/install.sh` restores a working entry.

## 4. Script coverage

- [x] 4.1 Add `scripts/install.test.ts` running both scripts through `Bun.spawn` with `PREFIX` always set to a fresh temporary directory (never the default, so the test cannot touch the operator's real `~/.local/bin`): cover the created link, the second run leaving exactly one entry, the uninstall refusal against a regular file, the refusal against a foreign symlink, the clean removal, and the empty case; verify with `bun test scripts/install.test.ts` green and `echo $?` recording the exit code.
`bun test scripts/install.test.ts` exit code 0, 11 passing, 0 failing, 32 `expect()` calls. Beyond the six cases this task names, five more pin the branches two review rounds found: an entry that is a symlink to a directory is replaced in place (nothing is written into the followed directory), a directory at the entry path is refused, installing through a symlinked path records the physical build output, and a failed build drops our own entry both when the artifact is gone and when a previous artifact still runs. Each was checked to fail against the pre-fix scripts: reverting `pwd -P` to logical `pwd` turns the symlinked-path case red, and restoring the old `[ ! -x $ARTIFACT ]` guard turns the runnable-previous-artifact case red. The spawned process also gets a temporary `HOME` alongside `PREFIX`, so a regression that ignored `PREFIX` could not reach the operator's real `~/.local/bin` either.
- [x] 4.2 Verify the test actually fails when a guard is broken: temporarily change the compared target in `scripts/uninstall.sh` (for example append a character), confirm `bun test scripts/install.test.ts` goes red naming the refusal case, then revert and confirm green again.

## 5. Documentation

- [x] 5.1 Add an install section to `README.md` covering: `bash scripts/install.sh` (and `bun run install:local`), the `PREFIX` default and the `PATH` line, the update recipe `git pull && bun run build`, `bash scripts/uninstall.sh`, the accepted failure mode where moving the clone leaves a dangling link repaired by re-running the installer from the new location, the manual `rm` needed when the clone is deleted outright, and the current scope (macOS arm64, host platform only); verify that every command quoted matches the scripts and `package.json` (`grep` each one against the files) and that the existing "Run" section still describes `bun start` accurately.
Each command checked against the file that defines it: `bash scripts/install.sh` and `bash scripts/uninstall.sh` are the `install:local` and `uninstall:local` values in `package.json`; `bun run build` is its `build` key, which `scripts/install.sh` also calls; `PREFIX=~/bin bash scripts/install.sh` matches `PREFIX="${PREFIX:-$HOME/.local/bin}"`; `rm ~/.local/bin/glabscope` is the documented manual step for a deleted clone. The documented `export PATH="$HOME/.local/bin:$PATH"` is the line the installer prints with the default target, which expands `$PREFIX` and so prints an equivalent absolute line. The "Run" section is untouched and still accurate: `start` is still `bun src/index.tsx`. One sentence beyond this task's list was added — that a failed build removes the installer's own entry — because task 2.5's behavior leaves the operator without a command and they need to know to re-run the installer.
- [x] 5.2 Verify the documented install path end-to-end by following the README literally in a fresh shell: `bash scripts/install.sh`, then `command -v glabscope` printing the installed path, then `bash scripts/uninstall.sh`, then `command -v glabscope` printing nothing; record the two outputs.
Recorded 2026-09-16, each step in its own fresh shell: after `bash scripts/install.sh` (exit 0), `command -v glabscope` printed `/Users/vantuh/.local/bin/glabscope`; after `bash scripts/uninstall.sh` (exit 0), `command -v glabscope` printed nothing. No `PATH` notice appeared, because the default target is already on `PATH`.

## 6. Integration verification

- [x] 6.1 Run `bun test` and `./node_modules/.bin/tsc --noEmit` and confirm both are green against the baseline measured before this change (249 passing, 0 failing, 20 files, 8.47 s; clean type check) plus the new `scripts/install.test.ts` cases; record the actual counts and any delta, and state the new file count.
Verified 2026-09-16: `bun test` 296 passing, 0 failing, 1165 `expect()` calls, 23 files, 10.47 s; `./node_modules/.bin/tsc --noEmit` exits 0. The baseline was re-measured at HEAD `f7bd93f` in a detached worktree without this change: 285 passing, 0 failing, 22 files, 9.33 s, and a clean type check — the 249 passing / 20 files / 8.47 s recorded when this plan was written had gone stale after the open-in-browser change landed. Delta from the re-measured baseline: +11 tests and +1 test file (`scripts/install.test.ts`) in +1.14 s, six covering the cases task 4.1 names and five pinning the branches the two review rounds found; no pre-existing test changed.
- [ ] 6.2 Operator check in a GitLab-bound working tree, because this checkout has no git remote and cannot bind to a project: install the command, run `glabscope` from a GitLab project and confirm the pipeline list appears, then `rm -rf node_modules` and run it again from the same project to confirm the command still works, then run it from a second GitLab project to confirm the project follows the working tree, then run it from a directory that is not a GitLab project and confirm the existing error appears; record the observed outcome in this change, or state it as not run.
Not run 2026-09-16: blocked by the missing `glab` login, not by the command. Every GitLab-bound checkout on this machine points at gitlab.com, whose login has no token — `glab ci list -F json` in all four of them exits with `404 Not Found`, and `glab auth status` reports 401 on `/user`; the instance that is authenticated (`gitlab.foodtech.team`) has no checkout on this machine, and this checkout has no remote at all. What could be reached was observed: run from the GitLab-bound `OPDWithdrawalFE` tree, which has no `node_modules` at all, the installed command still renders its frames and reports glab's own `404 Not Found` — so it needs neither the project's dependencies nor a companion file, and does not present an empty pipeline list as success; run from a directory that is not a GitLab project it shows the framed `Not a git repository` error. Still unverified, and left for the operator on a machine with a working GitLab login: the pipeline list rendering for a project that has pipelines, and the project following the working tree across two different GitLab projects.

## 1. Interval

- [x] 1.1 Set `IDLE_POLL_MS` in `src/polling.ts` to 15 s and leave `NORMAL_POLL_MS` (4 s) and `MAX_POLL_MS` (30 s) untouched; verify with `grep -rn "IDLE_POLL_MS\|NORMAL_POLL_MS\|MAX_POLL_MS" src` that the three constants are still defined only in `src/polling.ts` and consumed only from `src/app.tsx` and `src/polling.test.ts`, with no second hardcoded interval.

## 2. Guard the relationship

- [x] 2.1 Add assertions to `src/polling.test.ts` pinning `IDLE_POLL_MS` to 15 s, the ordering `NORMAL_POLL_MS < IDLE_POLL_MS < MAX_POLL_MS`, and the spec floor `IDLE_POLL_MS >= 2 * NORMAL_POLL_MS`; verify with `bun test src/polling.test.ts` green and by temporarily lowering `IDLE_POLL_MS` to `NORMAL_POLL_MS` to confirm the floor assertion fails.
- [x] 2.2 Assert in `src/polling.test.ts` that a rate-limited tick from the watch interval reaches the ceiling in one step (`nextPollDelay(IDLE_POLL_MS, true) === MAX_POLL_MS`) and that a successful tick from the watch interval returns the loop to 15 s; verify with `bun test src/polling.test.ts` green. `nextPollDelay` itself only clears the escalation back to `NORMAL_POLL_MS`; the 15 s re-application is the terminal-success branch of the list and graph loops in `src/app.tsx`, so the test asserts the primitive reset plus the loop's watch constant and names that branch in a comment rather than claiming loop coverage it does not have.

## 3. Verification

- [x] 3.1 Run `bun test` and `./node_modules/.bin/tsc --noEmit` and verify both are green with no new failures against the current baseline (173 passing tests, clean type check) plus the new `src/polling.test.ts` assertions. The baseline at apply time was 191 passing tests, not 173 - the in-flight `pipeline-list-columns` change had added tests since this plan was written. Result: 196 passing, 0 failing, `tsc --noEmit` clean. The four watch-interval assertions in `src/app.test.tsx` that hardcoded 5000 now read `IDLE_POLL_MS` and `NORMAL_POLL_MS`, so the interval has no second copy in the tests.
- [ ] 3.2 Run `bun start` from a GitLab-bound working tree and verify by eye that the footer spinner on a terminal pipeline list and graph appears roughly four times a minute while an active pipeline refreshes roughly fifteen times a minute, and that a job retried outside the TUI appears within one watch interval; record the observed outcome in this change, or state it as not run (this working copy has no git remote, so the interval cannot be exercised from here). **Not run.** `git remote -v` is empty in this checkout, so there is no GitLab project for `bun start` to bind to and the cadence cannot be observed here. Automated coverage stands in for the mechanics - the watch interval is scheduled at 15 s, one rate-limited step reaches the 30 s cap, and the loop returns to the normal interval once something is active again - but the by-eye refresh rate and the cross-TUI job-retry latency remain unverified until this runs in a GitLab-bound working tree.

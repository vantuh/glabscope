## 1. URL, Key, and Opener Foundations

- [x] 1.1 Add `src/gitlab-url.ts` with `pipelineWebUrl(base, pipelineId)` and `jobWebUrl(base, jobId)`: both trim a trailing slash from the base, reject an id that is not all digits before building anything, and produce `<base>/-/pipelines/<id>` and `<base>/-/jobs/<id>`. Verify `src/gitlab-url.test.ts` covers a base with and without a trailing slash, a non-numeric and an empty id rejected, and the two shapes matching the values glab itself reported on the operator's instance (pipeline id `1457442`, job id `47074530`, recorded in design.md).
- [x] 1.2 Add `isOpenKey(key)` to `src/keys.ts` beside the quit and retry predicates: true for `name === "o"` with no ctrl, meta, option, or super, and shift allowed so `Shift+O` counts too. Verify `src/keys.test.ts` covers plain `o`, `o` with shift, `o` with each of ctrl/meta/option/super set, and the other keys in use (`q`, `r`, `enter`, `escape`, arrows) still false.
- [x] 1.3 Add `src/browser.ts` with a pure `browserArgv(platform, url)` returning `["open", url]` for darwin, `["xdg-open", url]` for linux, `["cmd", "/c", "start", "", url]` for win32 and null for anything else, plus `openInBrowser(url)` that spawns that argv with stdin/stdout/stderr ignored and the process released, returning `false` (without throwing) when the platform is unsupported or the spawn fails, and `true` when the launch was started. Verify `src/browser.test.ts` covers each platform's argv, an unsupported platform returning null/false, and a spawn failure returning false rather than throwing (a missing binary name through the injected spawn, so no real browser opens during tests).

## 2. Project Web Address from the Existing Cached Read

- [x] 2.1 Extend the cached project read in `src/glab/graph.ts` from `path_with_namespace` to a record holding the full path and the optional `web_url` from the same `glab repo view -F json` payload, and export it for the open path while `fetchPipelineGraph` keeps using the full path exactly as before. Verify `src/glab/graph.test.ts` covers one `glab repo view` call shared by repeated reads of the same directory (cache hit), a payload with `path_with_namespace` but no `web_url` still yielding a usable full path with an absent web address, and `path_with_namespace` still required.

## 3. Model: Entering the Attempts Chooser from the Browser Key

- [x] 3.1 Add the new reducer action and its notice text to `src/model.ts`: from the job graph, a card whose job has more than one attempt opens the attempts screen with the newest attempt focused and stores a notice naming that the job has several attempts and that one must be picked and the open key pressed again; the same action on a card with a single attempt changes nothing. Verify `src/model.test.ts` covers the two-attempt card reaching `screen: "attempts"` with `focusedAttemptIndex 0` and the notice text, the single-attempt card leaving the model untouched, and the existing confirm-key action still storing no notice (so the confirm route stays silent).

## 4. Opening from Each Screen

- [x] 4.1 Wire the open key on the pipeline list: resolve the project web address, build the selected row's pipeline URL, and call the opener behind a single-in-flight guard, with a failed resolve or a failed launch recorded in the non-fatal notice; render the notice line on the list screen (which currently draws only refresh warnings). Verify `src/app.test.tsx` (with the opener and the project read spied) covers the selected row's own id in the opened URL, a historical second row opening that row's id rather than the first row's, two open presses in one frame producing one launch, a launch failure showing a non-fatal message with the rows still usable and no error screen, and the selection, screen, and list fetch count unchanged by the key.
- [x] 4.2 Wire the open key on the job graph: a card with one attempt opens that attempt's job URL without leaving the graph; a card with several attempts opens nothing and instead commits the chooser action from 3.1. Verify `src/app.test.tsx` covers one launch with the focused card's numeric job id, no launch plus the attempts screen and its notice for a retried card, no log screen and no retry/run call from the open key, and the graph still navigable after a failed launch.
- [x] 4.3 Wire the open key on the attempts list and the log screen: the attempts list opens the focused attempt's id (an older attempt opens as that older attempt, not the newest), and the log screen opens the traced attempt's id even when the refreshed graph no longer lists it. Verify `src/app.test.tsx` covers the older-attempt URL from the attempts list, the traced id from the log screen, a traced attempt absent from the graph still opening, and the live trace plus captured buffer unaffected by the key.
- [x] 4.4 Add the browser key to the four chrome help lines, and pin that the key does nothing else on any screen: no clipboard write from it on the log screen (that stays `y`), no confirmation prompt, and no extra list or graph fetch. Verify captured frames in `src/app.test.tsx` show the browser key in the list, graph, attempts, and log help rows, and that the same test file shows no clipboard write, no prompt, and an unchanged fetch count for an open press on each screen.

## 5. Docs and Change-Level Verification

- [x] 5.1 Add the browser key to the README key line, next to the existing wording, without rewriting the rest of the line. Verify the README diff shows only that wording change.
- [x] 5.2 Run `bun test` and `./node_modules/.bin/tsc --noEmit` and verify every test added or touched by this change passes and the type check is clean; report any pre-existing failure instead of fixing it here. Also confirm by reading `src/glab/graph.ts` and `src/glab/list.ts` that no per-press glab call and no new GraphQL field was added on any refresh path.
- [ ] 5.3 Exercise the change on a real project of the operator's GitLab: from the list, open a running pipeline and a historical one and confirm each browser tab shows that pipeline (not the latest on the ref); on the graph, open a job with a single attempt and confirm the tab is that job's page; on a retried job, press the open key, confirm nothing opens and the notice asks for a pick, then pick an older attempt and press the open key again and confirm the tab is that older attempt; open a log and confirm the tab is the traced job while the trace keeps streaming; confirm `o` and `Shift+O` behave the same and that `esc`/`q`/arrows/`y`/`ctrl+r` are unaffected. Record what was verified, including any part left unexercised.
